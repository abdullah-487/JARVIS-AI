[project]
name = "jarvis-ai"
version = "1.0.0"
description = "JARVIS AI Assistant"
requires-python = ">=3.11"

dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "google-genai",
    "tavily-python",
    "python-dotenv"
]

[tool.uv]
